server:
  port: 8091

spring:
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 10MB
  application:
    name: ms-upload

gcp:
  credentials:
    location: file:${GCP_BUCKET_CREDENTIALS_FILE_PATH}
  bucket-name: ${GCP_BUCKET_NAME}